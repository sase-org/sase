#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
repo_dir="$(cd -- "$script_dir/.." && pwd)"

if [[ -x "$repo_dir/.venv/bin/sase" ]]; then
  exec "$repo_dir/.venv/bin/sase" bead "$@"
fi

exec sase bead "$@"
