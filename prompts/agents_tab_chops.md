Can you help me add a new chop or two (add them to an existing lumberjack, or create a new one; you decide) that exist
to help optimize the "Agents" tab of the `sase ace` TUI? We can have that tab optionally use these files that are
generated when `sase axe` is running (ex: diff files, thoughts, diff command output, etc.). Navigating to that tab and
cycling through agent / workflow entries (using `j` and `k`) on that tab can be pretty slow. I'm hoping this will speed
things up.

This is a large piece of work that should be split into phases. I'll let you decide how many phases to create, but keep
in mind that each phase will be completed by a distinct `claude` instance.
