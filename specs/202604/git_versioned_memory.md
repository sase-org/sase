I want to add a custom git versioned memory system to sase that enables project-specific agent memory. Can you help me
implement this functionality? See the @research/git_versioned_agent_memory.md file, which contains some prior research,
for inspiration. This is a large piece of work that should be split into phases. I'll let you decide how many phases to
create, but keep in mind that each phase will be completed by a distinct agent instance (i.e. a distinct `claude` /
`gemini` / `codex` command). Think this through thoroughly and create a plan using your `/sase_plan` skill before making
any file changes.
