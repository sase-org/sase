description: Launch documentation refresh agents when a project has enough new commits.
hidden: true

input:
  project:
    type: line
    default: "sase"
    description: SASE project key whose documentation refresh marker should be checked.
  gh_ref:
    type: line
    default: "sase"
    description: GitHub ref or workspace selector to pass to the launched docs agents.
  threshold:
    type: int
    default: 100
    description: Minimum commit count since the marker required before launching a refresh.

steps:
  - name: count_commits
    hidden: true
    python: |
      import subprocess
      from pathlib import Path

      from sase.project_aliases import resolve_project_alias_ref

      project = resolve_project_alias_ref("{{ project }}")
      marker = Path.home() / ".sase" / "projects" / project / "refresh_docs_marker"
      head = subprocess.check_output(["git", "rev-parse", "HEAD"], text=True).strip()
      head_short = subprocess.check_output(["git", "rev-parse", "--short=12", "HEAD"], text=True).strip()
      threshold = {{ threshold }}

      if marker.exists():
          parts = marker.read_text().strip().split()
          last_sha = parts[0]
          last_ts = parts[1] if len(parts) > 1 else None
          check = subprocess.run(["git", "cat-file", "-t", last_sha], capture_output=True)
          if check.returncode == 0:
              result = subprocess.check_output(
                  ["git", "rev-list", "--count", f"{last_sha}..HEAD"], text=True
              )
              count = int(result.strip())
              base_kind = "sha"
              base_ref = last_sha
              range_desc = f"{last_sha}..{head}"
          elif last_ts:
              result = subprocess.check_output(
                  ["git", "rev-list", "--count", f"--after={last_ts}", "HEAD"], text=True
              )
              count = int(result.strip())
              base_kind = "timestamp"
              base_ref = last_ts
              range_desc = f"commits after unix timestamp {last_ts} through {head}"
          else:
              count = 999  # Old-format marker with no timestamp, force a refresh
              base_kind = "forced"
              base_ref = last_sha
              range_desc = f"forced refresh through {head}"
      else:
          count = 999  # First run, force a refresh
          base_kind = "first-run"
          base_ref = "none"
          range_desc = f"first refresh through {head}"

      print(f"project={project}")
      print(f"count={count}")
      print(f"head={head}")
      print(f"head_short={head_short}")
      print(f"marker={marker}")
      print(f"base_kind={base_kind}")
      print(f"base_ref={base_ref}")
      print(f"range_desc={range_desc}")
      print(f"threshold={threshold}")
      print(f"should_launch={str(count >= threshold).lower()}")
      if count < threshold:
          print("reason=below_threshold")
    output:
      count: int
      head: line
      head_short: word
      marker: line
      threshold: int
      should_launch: bool

  - name: launch_docs_agents
    hidden: true
    if: "{{ count_commits.count >= threshold }}"
    python: |
      import re
      import subprocess
      import textwrap

      from sase.agent.launcher import launch_agent_from_cwd

      project = "{{ project }}"
      gh_ref = "{{ gh_ref }}"
      safe_project = re.sub(r"[^A-Za-z0-9_.]+", "_", project).strip("_") or "repo"
      head = "{{ count_commits.head_short }}"
      name_prefix = f"refresh_docs.{safe_project}.{head}"

      docs_prompt = textwrap.dedent(f"""
      %name:{name_prefix}.update
      %w(runners=0)
      #gh:{gh_ref} %g:chop #sase/docs
      """).strip()

      polish_prompt = textwrap.dedent(f"""
      %name:{name_prefix}.polish
      %w(runners=0)
      #gh:{gh_ref} %g:chop #fork

      Inspect the documentation changes made by the first refresh-docs agent.
      Verify descriptions against the current system behavior rather than assuming they are true.
      Improve clarity for a new user, especially where terminology or workflow ordering could be misunderstood.
      Keep edits scoped to documentation unless a tiny sidecar correction is required.
      If you change files, run the appropriate documentation checks for this repository.
      """).strip()

      multi_prompt = f"{docs_prompt}\n---\n%wait\n{polish_prompt}"
      launch_agent_from_cwd(multi_prompt)
      print("launched=2")
      print(f"launched_agent_names={name_prefix}.update, {name_prefix}.polish")
    output: { launched: int }

  - name: update_marker
    hidden: true
    if: "{{ count_commits.count >= threshold and launch_docs_agents.launched > 0 }}"
    python: |
      import subprocess
      from pathlib import Path

      from sase.project_aliases import resolve_project_alias_ref

      project = resolve_project_alias_ref("{{ project }}")
      head = subprocess.check_output(["git", "rev-parse", "HEAD"], text=True).strip()
      ts = subprocess.check_output(["git", "log", "-1", "--format=%ct", "HEAD"], text=True).strip()
      marker = Path.home() / ".sase" / "projects" / project / "refresh_docs_marker"
      marker.parent.mkdir(parents=True, exist_ok=True)
      marker.write_text(f"{head} {ts}")
      print("updated=true")
      print(f"marker={marker}")
      print(f"head={head}")
    output: { updated: bool }
