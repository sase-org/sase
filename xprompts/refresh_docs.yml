# Retired: the refresh_docs workflow now lives in top-level workflows.refresh_docs
# config, for example in sase_athena.yml. This comment-only file intentionally
# does not define a loadable workflow.
