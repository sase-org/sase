---
plan: sdd/tales/202605/fast_manual_agents_refresh.md
---
 #resume:awj  Also, can you help me make the manual refresh, which is triggered by the `y` keymap, faster by querying the active agent index instead of whatever we are doing right now (it still takes ~3s to refresh, which is very slow)? Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
