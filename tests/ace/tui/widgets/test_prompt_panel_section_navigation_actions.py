"""Mounted prompt panel section navigation actions.

The legacy section-stop actions are unreachable on the Agents tab now that
decks are the only runtime path. The mounted cases were deleted by
sase-17d.10.1.1; `legacy-ui-deletion` (sase-17d.10.1.2) retires the actions.
"""
