[project]
name = "example-finalizers"
version = "1.0.0"
description = "Installable fixture plugin for SASE finalizer provider tests"
requires-python = ">=3.11"

[project.entry-points.sase_finalizers]
audit = "example_finalizers:provider"
