---
keyword: Service Node
---

A service node is one selectable row of the Services tab: a service-proc node, a oneshot
node, or — nested only under the Scheduler's node — a routine node and its job nodes. A
service-proc node is stable across process restarts; section dividers and the host
status line are chrome, not nodes.
