---
keyword: Service Node
---

A service node is one selectable row of the Services tab: a service-proc node or a
oneshot node in the Service Procs panel, or a routine node and its job nodes in the
Scheduled Routines panel. A service-proc node is stable across process restarts; panel
titles, section dividers, and the host status line are chrome, not nodes.
