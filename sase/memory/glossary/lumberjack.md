---
keyword: Lumberjack
---

An AXE lumberjack is an independently supervised scheduler process that runs its
configured chops concurrently on a fixed interval and records its own state and metrics.
The AXE orchestrator starts it and restarts it after crashes.
