---
keyword: LLM Calls
---

LLM Calls is the Agents detail and zoom view of normalized provider tool-call artifacts
(`tool_calls.jsonl` and compatible legacy rows). It is a presentation/read-model term
for what an LLM/provider did during an agent run, not an execution record: named SASE
tools and future ToolRuns are separate control-plane concepts.
