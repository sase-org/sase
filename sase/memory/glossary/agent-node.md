---
keyword: Agent Node
---

An agent node is the Agents-tab node for a non-dismissed sase agent: an agent session
node, or an agent shell node when the agent has no session. A session node's member rows
are sase shell nodes, not agent nodes. Dismissal removes the node, not the sase agent's
identity.
