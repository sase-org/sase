# Artifact Relation Registry

Typed artifact links use this closed relation registry. Agents write deliberate links with `sase artifact link add <source> <relation> <target> "<why>"`; prompt citations and audited reads use the same row shape.

## Relations

{{ artifact_relation_rows }}

## Reserved

The following slugs are scheduling concepts, not artifact-link relations:

{{ reserved_relation_rows }}
